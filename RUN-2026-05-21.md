# Shaper harness recovery/run — 2026-05-21

## Repairs made

- Repointed recovered eval harness testcase paths from stale `../skills/shaper-autorepair/testcases` to `../shaper-autorepair/testcases`.
- Added `providers.js`, a small local Ollama provider shim, because the handoff artifact did not include `../cc-lib/build/providers.js`.
- Set Ollama chat requests to `think: false`; without this, `kimi-k2.5:cloud` spent tiny test calls entirely in `message.thinking` and returned empty `message.content`.
- Exported `runBasicTest`, added a local regression test, and made unsupported problems fail closed instead of silently passing.
- Added basic MinStack test coverage.
- Restored N4 default problems to match the held-out N4 set:
  - `binary-search`
  - `climbing-stairs`
  - `container-with-most-water`
  - `coin-change-ii`
- Fixed `node index.js --run ...` help text falling through mid-run.
- Fixed `node index.js --compare` crash (`const row` mutated inside report loop).

## Verification

Commands passed:

```bash
node test-basic-runner.js
node test-ts-to-py.js
node test-spec-validator.js
node index.js --status
node index.js --compare
```

Ollama availability checked on `127.0.0.1:11434`; available cloud models included `kimi-k2.5:cloud`.

## Fresh N4 run result

Model: `kimi-k2.5:cloud`

- `raw_base`: pass@1 `4/4`, pass@N `4/4`
- `gen0_seed`: pass@1 `3/4`, pass@N `4/4`
- `gen18_evolved`: pass@1 `2/4`, pass@N `4/4`

Per-problem:

- `binary-search`: raw `✓/✓`, gen0 `✓/✓`, gen18 `✓/✓`
- `climbing-stairs`: raw `✓/✓`, gen0 `✓/✓`, gen18 `✗/✓`
- `container-with-most-water`: raw `✓/✓`, gen0 `✗/✓`, gen18 `✗/✓`
- `coin-change-ii`: raw `✓/✓`, gen0 `✓/✓`, gen18 `✓/✓`

## Notable failures/retries

- `gen0_seed / container-with-most-water`
  - attempt 0: assertion failed
  - attempt 1: shaper timeout at `25000ms`
  - attempt 2: pass
- `gen18_evolved / climbing-stairs`
  - attempt 0: shaper produced no JSON
  - attempt 1: pass
- `gen18_evolved / container-with-most-water`
  - attempt 0: coder produced no code after 1 autorepair cycle
  - attempt 1: pass

## Current caveat

This N4 run is now executable and recorded, but the result is directionally awkward: raw baseline beat the shaper pipelines on pass@1. Treat this as a harness/measurement signal to investigate, not as a final claim about Shaper quality.

Likely next harness work:

1. Add variance runs / repeated seeds before interpreting pass@1.
2. ~~Add structured logging for raw model outputs on shaper/coder parse failures.~~ Done in checkpoint below.
3. Expand `runBasicTest` coverage for the 12 expansion problems before running the full 16-problem set.
4. Revisit prompt budgets/timeouts: Kimi sometimes returns no JSON/no code or times out at 25s on shaper stages.

## Checkpoint: ARE-guided trace logging — 2026-05-21

Used an ARE/ERAS stress-test frame (`RCR`, `PERM_GRAD`, `COH_ATR`, `STRUCT_GEN`) to avoid over-interpreting the N4 result. Verdict: do **not** conclude raw baseline is better yet; first separate model formatting/protocol failures from solution logic failures.

Changes made:

- Added `trace-log.js` with JSONL trace writing and bounded raw-output snippets.
- Added `test-trace-log.js` and verified RED → GREEN before integrating.
- Wired `evalProblem(...)` / `runPipeline(...)` to attach `traceLog` paths to attempts.
- `run-logs/<problem>-<baseline>.jsonl` now records bounded snippets for:
  - `shaperRaw`
  - `coderRaw`
  - `verifierRaw`
  - extracted `code`
  - parsed `spec`
  - validation/test/parse/compile feedback where applicable
- Fixed a latent verifier-parse bug where the catch block referenced `vResult` outside scope.

Verification passed:

```bash
node test-trace-log.js
node test-basic-runner.js
node test-ts-to-py.js
node test-spec-validator.js
node index.js --compare
node index.js --status
```

Integrated smoke test:

- Ran `evalProblem('binary-search', 'raw_base', 'kimi-k2.5:cloud', { traceDir: /tmp/... })`.
- Verified trace file was written with `coderRaw` and extracted `code` snippets.

Next best move:

1. Add metrics that classify attempt failures as `format/protocol`, `logic/assertion`, `timeout`, `spec_validation`, or `model_error`.
2. Then run k-replicates; otherwise variance will still blur failure causes.

## Checkpoint: failure-kind metrics — 2026-05-21

Used the new ARE-v2 compiler on the literal `<task> continue working on shaper system </task>` prompt. It correctly treated `<task>` tags as untrusted plain text (`auto_execute: false`) and did not spawn work automatically. Parent agent selected the next safe harness step: classify pass@1 misses before running more variance.

Changes made:

- Added `failure-metrics.js` with deterministic attempt classification:
  - `logic_assertion`
  - `format_protocol`
  - `timeout`
  - `spec_validation`
  - `model_error`
  - `pass`
- Added `test-failure-metrics.js` with RED → GREEN coverage for the classifier, summary counts, and stable display labels.
- Extended `trace-log.js` / `test-trace-log.js` so future JSONL trace rows preserve `failureKind`.
- Wired `evalProblem(...)` attempts to include `failureKind` for new runs.
- Updated `index.js --compare` to print attempt-kind summaries for existing and future state data.
- Updated `state.js` metadata/types to include `failureKind`.

Current N4 failure-kind readout from existing state:

- `raw_base`: pass=4, logic/assertion=0, format/protocol=0, timeout=0, spec_validation=0, model_error=0
- `gen0_seed`: pass=4, logic/assertion=1, format/protocol=0, timeout=1, spec_validation=0, model_error=0
- `gen18_evolved`: pass=4, logic/assertion=0, format/protocol=2, timeout=0, spec_validation=0, model_error=0

Verification passed:

```bash
node test-failure-metrics.js
node test-trace-log.js
node test-basic-runner.js
node test-ts-to-py.js
node test-spec-validator.js
node index.js --compare
node index.js --status
```

Next best move:

1. Run k-replicates now that pass@1 misses are bucketed by cause.
2. Expand `runBasicTest` coverage for the 12 expansion problems before making broad 16-problem claims.
