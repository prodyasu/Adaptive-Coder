Given a string s, return true if s is a palindrome when considering only alphanumeric characters and ignoring case.

Examples:
- "A man, a plan, a canal: Panama" → true
- "race a car" → false

Constraints:
- 1 <= s.length <= 2 * 10^5
- s consists only of printable ASCII characters
